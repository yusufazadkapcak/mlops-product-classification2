# Railway deployment configuration
[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/Dockerfile.inference"

[deploy]
startCommand = "python -m uvicorn src.inference.api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10


